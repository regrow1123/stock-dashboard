services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
      - ./seed:/app/seed:ro
      - ${HOME}/.claude:/root/.claude   # reuse host claude auth
